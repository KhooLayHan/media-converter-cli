<configuration>
	<!-- Suppress logback's internal status messages from console -->
	<statusListener class="ch.qos.logback.core.status.NopStatusListener" />

	<!-- Property for log file location - makes it configurable -->
	<property name="LOG_DIR" value="${LOG_PATH:-logs}"/>
	<property name="LOG_FILE" value="${LOG_DIR}/media-converter.log"/>
	<property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

	<!-- Console Appender with colored output, only for development/debugging -->
	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{20}) - %msg%n</pattern>
		</encoder>
	</appender>

	<!-- Rolling File Appender for all Application Logs -->
	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_FILE}</file>
		<encoder>
			<pattern>${LOG_PATTERN}</pattern>
		</encoder>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- Daily rollover -->
			<fileNamePattern>${LOG_DIR}/media-converter.%d{yyyy-MM-dd}.log</fileNamePattern>
			<!-- Keep 30 days of history -->
			<maxHistory>30</maxHistory>
			<!-- Total size cap -->
			<totalSizeCap>5GB</totalSizeCap>
		</rollingPolicy>
	</appender>

	<!-- Separate file for errors only -->
	<appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_DIR}/media-converter-error.log</file>
		<encoder>
			<pattern>${LOG_PATTERN}</pattern>
		</encoder>
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>ERROR</level>
		</filter>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${LOG_DIR}/media-converter-error.%d{yyyy-MM-dd}.log</fileNamePattern>
			<maxHistory>90</maxHistory>
			<totalSizeCap>2GB</totalSizeCap>
		</rollingPolicy>
	</appender>

	<!-- Async appender for better performance -->
	<appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
		<appender-ref ref="FILE"/>
		<queueSize>512</queueSize>
		<discardingThreshold>0</discardingThreshold>
	</appender>

	<appender name="ASYNC_ERROR_FILE" class="ch.qos.logback.classic.AsyncAppender">
		<appender-ref ref="ERROR_FILE"/>
		<queueSize>256</queueSize>
		<discardingThreshold>0</discardingThreshold>
	</appender>

	<!-- Root logger -->
	<root level="INFO">
		<appender-ref ref="ASYNC_FILE"/>
		<appender-ref ref="ASYNC_ERROR_FILE"/>
	</root>

	<!-- Application specific loggers -->
	<logger name="org.khoolayhan.mc" level="DEBUG" additivity="false">
		<appender-ref ref="ASYNC_FILE"/>
		<appender-ref ref="ASYNC_ERROR_FILE"/>
	</logger>

	<!-- Development for console logging -->
	<!-- Uncomment this during development -->
	<!--
	<logger name="org.khoolayhan.mc" level="DEBUG" additivity="false">
	   <appender-ref ref="CONSOLE"/>
	   <appender-ref ref="ASYNC_FILE"/>
	   <appender-ref ref="ASYNC_ERROR_FILE"/>
	</logger>
	-->

	<!-- Reduce noise from third-party libraries/frameworks -->
	<logger name="org.springframework" level="WARN"/>
	<logger name="org.hibernate" level="WARN"/>
	<logger name="com.zaxxer.hikari" level="WARN"/>
	<logger name="picocli" level="WARN"/>
</configuration>
